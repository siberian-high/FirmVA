# FirmVA
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-mcp-adapters>=0.1.0
pydantic>=2.0
python-dotenv>=1.0
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
requests>=2.31

# QEMU MCP Server (mcp/qemu/server.py)
fastmcp>=0.2.0

# binwalk, Ghidra, QEMU는 pip 패키지가 아닌 시스템 도구이므로 별도 설치
# binwalk : snap install binwalk
# Ghidra  : 공식 배포본 + LaurieWired/GhidraMCP 플러그인
# QEMU    : apt-get install qemu-system-arm